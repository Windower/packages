<package>
  <name>equipment_service</name>
  <version>0</version> <!-- 2nd 3rd and 4th numbers are optional, can also have an optional tag string suffix -->
  <type>service</type> <!-- can also be "service" or "addon" -->
  <dependencies>
    <dependency>resources_service</dependency>
    <dependency>reading</dependency>
    <dependency>sharing</dependency>
    <dependency>pack</dependency>
  </dependencies>
</package>
