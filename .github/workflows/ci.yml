name: Continuous Integration

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

defaults:
  run:
    shell: pwsh

jobs:
  version:
    name: Version Check
    runs-on: windows-2019
    steps:
    - name: Checkout
      uses: actions/checkout@v2
