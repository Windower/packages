<package>
  <name>evaluate</name>
  <version>1</version> <!-- 2nd 3rd and 4th numbers are optional, can also have an optional tag string suffix -->
  <type>addon</type> <!-- can also be "library" or "service" -->
  <dependencies>
    <dependency>reading</dependency>
    <dependency>resources_service</dependency>
    <dependency>inventory_service</dependency>
    <dependency>equipment_service</dependency>
    <dependency>player_service</dependency>
  </dependencies>
</package>