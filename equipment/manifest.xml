<package>
  <name>equipment</name>
  <version>0</version> <!-- 2nd 3rd and 4th numbers are optional, can also have an optional tag string suffix -->
  <type>library</type> <!-- can also be "service" or "addon" -->
  <dependencies>
    <dependency>enumerable</dependency> 
    <dependency>resources</dependency> 
    <dependency>items_service</dependency>
  </dependencies>
</package>